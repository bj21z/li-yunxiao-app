# 云上小百花·李云霄 V2.1.0

# 最终稳定版发布说明

## 本次调试重点

- 修复统一工程两个演员子应用 `app.js` 中实时接口调用的 JavaScript 语法错误；该错误会导致整份脚本停止执行，是后两个产品不稳定的主要原因。
- 重写 Service Worker 缓存策略，避免安装阶段因单个资源失败而整体失效。
- 为不同演员应用设置独立缓存命名空间，避免同一域名下相互清理或读取对方缓存。
- 将通用本机存储键改为演员独立命名空间，避免收藏、签到、主题、云笺、资讯缓存互相串用。
- 导航页面采用联网优先、离线回退；JSON/API 采用联网优先；静态资源采用缓存优先并后台刷新。
- 清理压缩包内 `.git` 仓库数据，避免 Code App 克隆、初始化、提交和推送冲突。
- 更新资源版本号，强制浏览器识别新版 CSS、JavaScript 和 Service Worker。
- 完成 JavaScript、Service Worker、Cloudflare Function、自动更新脚本和全部 JSON 的语法/格式检查。

## 部署要求

本版必须全量替换。不要把压缩包外层目录直接套入原项目形成双层文件夹；应复制压缩包内项目根目录的全部内容，覆盖目标仓库根目录。

发布后请关闭原网页标签并重新进入。若仍显示旧版，清除该网站缓存；已添加到主屏幕的旧图标建议删除后重新添加。

## 已知边界

第三方在线图片和外部平台页面仍可能受来源站防盗链、登录、验证码、链接调整或网络线路影响。程序已做失败占位和主体功能隔离，但无法控制第三方服务器。

## 本产品专项说明

独立版缓存前缀：`liyunxiao-`。
